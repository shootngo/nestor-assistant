# Kitchen brain key. Copy to `.env` and fill in. Never commit `.env`.
# Get a key at https://aistudio.google.com/apikey
# Rebuild the APK after changing this — Expo inlines EXPO_PUBLIC_ values at bundle time.
EXPO_PUBLIC_GEMINI_API_KEY=
